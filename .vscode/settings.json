{
    // Add the local `typings` folder so Pylance can resolve workspace stubs
    "python.analysis.extraPaths": [
        "${workspaceFolder}/typings"
    ],
    "cursorpyright.analysis.extraPaths": [
        "${workspaceFolder}/typings"
    ]
}