from app import app

# This file acts as the entry point for Vercel serverless functions.
# It imports the core Flask app from app.py and exposes it.
# The serverless runtime will treat this as the function handler.
